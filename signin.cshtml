@{

}

<!DOCTYPE html>

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title></title>
    </head>
    <body>
        @{
          string name = Request["usrname"];
          string pass = Request["usrpwd"];
 var db = Database.Open("SaanjhiDrive-master"); 
 var selectQueryString = "SELECT usrname, usrpwd FROM users"; 
 var count=0;
 foreach(var row in db.Query(selectQueryString))
 {
     if (row.usrname==name && row.usrpwd==pass)
     {count++;}
    //else
    //{<p>Sorry, you are not valid user, Please try again.</p>}
 }  
 if(count>0)
 {
     Response.Redirect("mainpage.html");
 }
 else
 {
     Response.Redirect("index.html");
     
    // <p>Sorry, you are not valid user, Please try again.</p>
 }

        }
    </body>
</html>
